create a way to represent a version as a numeric value so that it can be compared to other versions. This allows a database back-end to be used more easily.

Create selector/modifier API to be used in place of profiles
